const notifications = require("./../assets/icons/notif.png")
const home = require("./../assets/icons/home.png")
const cart = require("./../assets/icons/basket.png")
const favorite = require("./../assets/icons/heart.png")

const icons = { 
    notifications, 
    home,
    favorite,
    cart  
}

export default icons